# 開発フロー: Issue駆動開発

このプロジェクトは現場に近い開発フローで進めるため、Issue駆動開発を採用する。

## ルール

1. **実装に取り掛かる前に、GitHub Issueを作成する**
   - 粒度は機能単位（例:「レシピ登録API実装」「レシピ一覧画面実装」）
2. **Issue番号を含むブランチ名で作業する**
   - 命名規則: `issue-<Issue番号>-<内容を表す短い英語>`（例: `issue-12-recipe-register-api`）
3. **PR本文に `Closes #<Issue番号>` を記載する**
   - PRがマージされると、対応するIssueが自動でクローズされる

## Claudeへの指示

- 新しい実装タスクに着手する前に、対応するIssueが存在するか確認する。存在しなければ、Issueの作成を提案してから進める
- ブランチを切る際は上記の命名規則に従う
- PRを作成する際は、本文に `Closes #<Issue番号>` を含める
- Issue作成・PR作成・pushなど、GitHub上に影響する操作は、実行前に必ずユーザーに確認を取る

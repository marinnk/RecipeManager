# CLAUDE.md

Claude Codeがこのリポジトリで作業する際に必ず守るルールです。

> **注記について**: 各ルールの下にある `> なぜ:` から始まる引用ブロックは、そのルールを設けた理由を説明する注記です。指示そのものではないので、ルールの内容と混同しないでください。

## 開発フロー（Issue駆動開発）

コード変更を伴うタスクに着手する際は、必ず以下の順序で作業してください。ユーザーから明示的に「Issueを立てて」と指示されていなくても、このフローに従います。

> なぜ: 「思いついたらすぐコードを書く」のではなく、着手前に目的・完了条件をIssueとして言語化することで、作業のゴールがぶれにくくなる。また、後から「この変更は何のために入れたのか」をGitHubの履歴だけで追えるようにする狙いもある。

1. **Issueを起票する**

   ```sh
   gh issue create --title "<タイトル>" --body "<概要・完了条件>"
   ```

   タイトルは何をするか一目で分かる内容にし、本文には概要・背景・完了条件を記載します。粒度は機能単位とします（例:「レシピ登録API実装」「レシピ一覧画面実装」）。

   > なぜ: 粒度を機能単位にするのは、1つのPRが大きくなりすぎるとレビューしづらく、途中で問題が起きたときに原因を切り分けにくくなるため。「API」と「画面」で分けた例が挙げられているのは、着手した当初はバックエンドとフロントエンドを別Issueにする方針だったため（後にTaskManagementプロジェクトでの運用実績を踏まえ、機能単位でバックエンド〜フロントエンドをまとめる方針に変わった経緯がある）。

2. **Issueに対応するブランチを作成する**

   命名規則: `<type>/<issue番号>-<短い英語slug>`

   - `type` は `feature` / `fix` / `chore` / `docs` / `refactor` のいずれか
   - 例: `feature/12-recipe-register-api`, `fix/15-fix-tag-search`

   ```sh
   git switch -c feature/12-recipe-register-api main
   ```

   > なぜ: ブランチ名にIssue番号を入れておくと、ブランチ名だけで「どのIssueに対応する作業か」がGitHub上でもローカルでもすぐわかる。`type`を接頭辞にするのは、コミットメッセージ規約（Conventional Commits）と揃えることで、変更の性質（新機能か・修正か・雑務かなど）を統一的に判断できるようにするため。

3. **作業ブランチ上で変更・コミットする**

   - **mainブランチへの直接コミット・直接pushは禁止**です。必ず作業ブランチ上で作業してください。
   - mainブランチにはGitHub側で保護ルール（PR必須・force-push禁止・削除禁止、管理者にも適用）が設定されているため、直接pushは技術的にも拒否されます。
   - ドキュメントの誤字修正など、極めて軽微でリスクのない変更であっても、mainへの直接pushはできないため、このフローに従ってください。

   > なぜ: mainに誰でも直接pushできると、動作確認前のコードや誤った変更がそのまま本番相当のブランチに混ざってしまうリスクがある。「軽微でも例外を作らない」のは、一度でも例外を許すと「これくらいならいいか」の基準が曖昧になり、ルール自体が形骸化しやすいため。ブランチ保護もGitHub側の設定で技術的に強制しているので、うっかりミスでも防げるようになっている。

4. **PRを作成する**

   ```sh
   gh pr create --title "<タイトル>" --body "Closes #<issue番号>

   <変更内容の概要>"
   ```

   本文に `Closes #<issue番号>` を含め、マージ時にIssueが自動でクローズされるようにします。`.github/pull_request_template.md` の項目を埋めてください。

   > なぜ: `Closes #<issue番号>`と書くと、GitHubがPRのマージを検知して自動でIssueをクローズしてくれる。手動でIssueを閉じ忘れる、というありがちなミスを防げる。PRテンプレートを埋めるのは、レビューする側（自分自身や将来の自分）が「何を」「どう確認したか」を毎回同じ形式で把握できるようにするため。

5. **マージはユーザーの承認を得てから行う**

   PRのマージ（`gh pr merge`）は共有状態への変更にあたるため、ユーザーに明示的に確認を取ってから実行してください。無断でマージしないこと。

   > なぜ: マージするとmainブランチの内容が確定し、後から取り消すには追加の操作（revertなど）が必要になる。取り消しにくい操作だからこそ、実行前に一呼吸置いて人間の最終確認を挟む。CIが通っていることと「マージしてよいか」は別の判断（例えば動作確認がまだ済んでいない、など）なので、CI通過だけでは自動マージしない。

6. **マージ確認後、mainに戻ってローカルの作業ブランチを削除する**

   PRがマージされたことを確認したら、指示を待たずに以下を行ってください。

   ```sh
   git switch main
   git pull --ff-only
   git branch -d <作業ブランチ名>
   ```

   リモートの作業ブランチはGitHub側の設定でマージ後に自動削除されるため、削除操作は不要です。

   > なぜ: マージ済みブランチをローカルに残し続けると、後で「このブランチはまだ必要だったか」の判断が面倒になり、ローカル環境が散らかっていく。マージ確認後は毎回同じ後片付けなので、都度指示を待たず自動でやってよいこととして明文化してある。

7. **ユーザーが「コミットプッシュして」のように明示的に依頼した場合**

   作業ブランチへのコミット・pushは確認を取らずに実行してよい。Issue作成・PR作成・PRのマージなど、GitHub上でmainに影響する操作は、実行前に必ずユーザーに確認を取る。

   > なぜ: 作業ブランチへのコミット・pushは、mainやIssue/PRの状態そのものには影響しない（＝取り消しやすい・他人に見える公式な状態ではない）ため、都度確認を挟まなくても実害が小さい。一方でIssue作成・PR作成・マージはGitHub上に見える形で記録が残り、他者（講師など）から見える状態になりうるため、確認を挟む対象として区別している。

## コミットメッセージ規約

[Conventional Commits](https://www.conventionalcommits.org/) 形式を採用する。

> なぜ: `git log`を見ただけで「これは新機能の追加か、バグ修正か、ただの整理か」が一目でわかるようにするため。個人開発でも、後から「あの変更どこでやったっけ」を探すときに役立つ。世の中で広く使われている規約に合わせておけば、他のプロジェクトを見るときにも読み方に迷わない。

| プレフィックス | 内容 |
|----------------|------|
| `feat:` | 新機能 |
| `fix:` | バグ修正 |
| `docs:` | ドキュメントのみの変更 |
| `refactor:` | 挙動を変えないコードの整理 |
| `test:` | テストの追加・修正 |
| `chore:` | ビルド設定・依存関係など、上記に当てはまらない変更 |

例: `feat: レシピ登録APIを実装`

## コーディング規約

- 新しい機能を実装する際は、対応する自動テストも合わせて実装してください。
  - バックエンド：JUnit5 + MockK でサービス層の単体テストを書く（`./gradlew test`）
  - フロントエンド：Vitest + React Testing Library でコンポーネント・ロジックのテストを書く（`npm run test`）

  > なぜ: テストが無いと、後から別の機能を追加したときに「今までの機能が壊れていないか」を毎回手動でブラウザを開いて確認することになり、手間がかかる上に確認漏れが起きやすい。サービス層（バックエンドの業務ロジック）を優先してテストするのは、画面や通信の細部より「正しい結果を計算できているか」がバグの影響として大きく、かつDBやHTTP通信を用意せずにテストしやすい層だから。

- PRを作成する前に、フロントエンド（`npm run lint` / `npm run test`）・バックエンド（`./gradlew test`）を実行し、エラーが無いことを確認してください。
- 上記はGitHub Actions（CI）により、push・PR作成のたびに自動実行されます。CIが落ちた状態でのマージはしないでください。

  > なぜ: ローカルで確認せずにPRを出すと、CIが落ちてから気づくことになり、修正のやり取りが増えて手戻りが大きくなる。先に手元で確認しておけば、CIは「見落としがないかの最終チェック」として機能する。CIが落ちたままマージしないのは、動くかどうか確認できていないコードをmainに混ぜないため（ルール5「マージの承認」とも繋がる考え方）。

- Lint / フォーマッタ：バックエンドは [ktlint](https://pinterest.github.io/ktlint/)、フロントエンドはESLint + Prettierを使う。

  > なぜ: 複数のファイルで書き方（インデント・改行位置・命名など）がバラバラだと、コードを読むたびに「これはどういう意図の違いか」を考える余計な負担が生まれる。Lint/フォーマッタで機械的に揃えておけば、その負担をなくし、本質的なロジックの違いだけに集中できる。

- コンポーネント・関数は単一の責務に絞ってください。1つのファイルにデータ取得・状態管理・画面表示など複数の役割を詰め込まないこと。
  - フロントエンド：APIとの通信・データ更新のロジックはカスタムフック（`useXxx`）に分離し、コンポーネントは受け取ったデータをどう表示するかに専念させる（Spring Bootでいう、業務ロジックをServiceに寄せてControllerを薄く保つのと同じ考え方）

  > なぜ: 役割を分けておくと、(1) 通信部分だけ・表示部分だけ、というように狭い範囲でテストできる、(2) 片方だけを直したいときにもう片方を巻き込まずに済む、(3) 同じロジックを別の画面からも再利用できる、(4) 不具合が起きたときに「症状」から見るべきファイルの見当がつけやすい、といった効果がある。1つのファイルに全部詰め込むと、この4つがすべてやりづらくなる。

上記のテスト・Lintツールは開発環境構築のタイミングで実際に導入する。

## 動作確認のためにサーバーを起動する場合

バックエンド・フロントエンドの起動手順は `.claude/skills/run-app/SKILL.md` を参照してください。特に以下は必ず守ること。

- バックエンドは必ず **8080**、フロントエンドは必ず **3000**、DB（MySQL）は必ず **3306** で起動する。ポートが競合したときに、別のポートで起動された状態のまま「動いている」と判断してはいけません。
- ポート競合が起きた場合は、そのポートを使っているプロセスを停止してから、既定のポートで起動し直してください。別ポートでの一時起動でごまかさないこと。

> なぜ: フロントエンドからバックエンドAPIへの接続先や、バックエンド側のCORS許可オリジンは固定ポート前提で設定される想定になっている。別のポートで一時的に動かして「動いた」ように見えても、実際の設定と違う環境で確認したことになり、正しい動作確認になっていない可能性がある。ポートを固定しておくことで、「動作確認した」という結果に再現性を持たせている。

## 例外

- 複数の関連する変更を1つのIssue・PRにまとめても構いませんが、無関係な変更を混ぜないでください。

  > なぜ: 関連する変更を細かく分けすぎると、逆にPRの数が増えて追いにくくなる場合がある一方、無関係な変更を混ぜると「このPRは結局何をしたものか」が説明しづらくなり、後から特定の変更だけを取り消したい（revertしたい）ときにも困る。「関連しているかどうか」を基準にすることで、極端に細かすぎず、雑多にもならないバランスを取っている。
